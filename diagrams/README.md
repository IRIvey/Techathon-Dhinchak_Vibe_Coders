# Diagrams

Place the following here before submission:

1. `system-diagram.png` (or .pdf) -- high-level flow: Simulated Device Layer -> Backend API -> Web Dashboard + Discord Bot -> User.
   Build in draw.io, Excalidraw, or by hand. **Do not use Mermaid** (per problem statement).

2. `circuit-schematic.png` -- exported/screenshotted from your Wokwi project showing one room's
   wiring (ESP32 + relay module + LEDs representing lights/fans, optionally an ACS712 current
   sensor). Include the Wokwi project link in the main README as well.
